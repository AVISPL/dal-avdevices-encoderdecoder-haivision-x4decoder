# dal-avdevices-encoderdecoder-haivision-x4decoder
DAL adapter for Haivision Makito X4 Video Decoder

**Build project with no tests: ``` mvn clean install -DskipTests```**

**Build project with all tests: ``` mvn clean install ```**